name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.5'
      SERVERPOD_PASSWORD_database: 'd4x_A-1trBbq0lE8zfS00fe4bEtxP0T2'
      SERVERPOD_PASSWORD_redis: '0I24wZ768B4spTPMK23dVerai3ViGvo2'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: chat_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: chat_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: chat_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: chat_server
        run: serverpod generate

      - name: Run tests
        working-directory: chat_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: chat_server
        run: docker compose down -v
